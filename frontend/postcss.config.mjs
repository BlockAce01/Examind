// postcss.config.mjs
const config = {
  plugins: {
    tailwindcss: {}, // Use the main tailwindcss plugin
    autoprefixer: {}, // Add autoprefixer for browser compatibility
  },
};

export default config;