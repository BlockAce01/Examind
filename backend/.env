# backend/.env sadasdsaas

# PostgreSQL Connection
DB_USER=postgres             # Replace with your PostgreSQL username
DB_HOST=localhost            # Replace with your DB host (e.g., localhost or cloud host)
DB_DATABASE=examind-db       # Replace with your database name
DB_PASSWORD=paka             # Replace with your PostgreSQL password
DB_PORT=5432                 # Default PostgreSQL port

# JWT Secret (Choose a long, random, secure string)
JWT_SECRET=Aliyanna12%56?3456sadasdsad7890!@

# Server Port (Optional, defaults to 3001 in server.js if not set)
# PORT=3001